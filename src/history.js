// import createHistory from 'history/createBrowserHistory'; same as below.Avoid some Error message in Console.
const createHistory = require("history").createBrowserHistory

export default createHistory();